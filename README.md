# Hyprkeys

I'm always forgetting my keys. This lets me look them up.

## LOL What?

This is a stupid program that parses your `~/.config/hypr/hyprland.conf` file and outputs a table of the keybindings. It is hardcoded to parse `~/.config/hypr/hyprland.conf`.
